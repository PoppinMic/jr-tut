const initialState = {
  isFetched: false,
  data: null
}

const forecastReducer = (state=initialState, action)=>{
  switch(action.type){
    default:
      return state;
  }
}

export default forecastReducer
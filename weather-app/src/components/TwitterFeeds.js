import React from 'react';

import styles from './TwitterFeeds.module.scss'

const NewsFeed = props => (
  <div id="news-feed" className={styles.wrapper}>
    <div>:) News #city</div>
    <div>data... data...</div>
    <div>Next</div>
  </div>
);

export default NewsFeed;
